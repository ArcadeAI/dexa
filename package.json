{
  "name": "dexa-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:libs && npm run build:apps",
    "build:libs": "npm run build -w @dexa/core && npm run build -w @dexa/agent",
    "build:apps": "npm run build -w @dexa/api && npm run build -w @dexa/cli && npm run build -w @dexa/web --if-present && npm run build -w @dexa/site --if-present && npm run build -w @dexa/docs --if-present && npm run build -w @dexa/desktop --if-present && npm run build -w @dexa/mobile --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules"
  },
  "devDependencies": {
    "@types/node": "^25.0.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
